attribution_os:
  target: ci
  outputs:
    ci:
      type: postgres
      host: localhost
      port: 5432
      user: attrib
      password: attrib
      dbname: attrib_analytics
      schema: public
      threads: 4
      connect_timeout: 10
